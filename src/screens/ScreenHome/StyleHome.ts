import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 100,
        padding: 50,
        backgroundColor: "#23f"
    },
    text: {
        color: 'white',
        fontWeight: "400"
    }

});

export default styles;